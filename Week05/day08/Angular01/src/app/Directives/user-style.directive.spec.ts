import { UserStyleDirective } from './user-style.directive';

describe('UserStyleDirective', () => {
  it('should create an instance', () => {
    const directive = new UserStyleDirective();
    expect(directive).toBeTruthy();
  });
});
